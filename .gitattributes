# Keep the repo LF-normalised: lesson content lives in template literals that
# are compared and compiled, so consistent line endings avoid noisy diffs.
* text=auto eol=lf
*.png binary
*.jpg binary
*.wasm binary
*.woff2 binary
# The hook must stay LF even on a Windows checkout, or sh chokes on \r.
.githooks/* text eol=lf
