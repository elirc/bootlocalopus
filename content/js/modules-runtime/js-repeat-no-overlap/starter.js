export function repeat(task, intervalMs, { timers = { setTimeout, clearTimeout }, onError } = {}) {
  // TODO: a fixed-delay loop through timers.setTimeout, with a stop()
  // that waits for the run in progress.
  throw new Error('repeat: not implemented');
}
