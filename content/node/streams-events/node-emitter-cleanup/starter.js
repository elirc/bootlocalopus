export function watchJob(emitter, jobId, { signal } = {}) {
  // TODO: subscribe now, queue this job's events, and hand them out through an
  // async iterator that removes every listener on every exit.
  throw new Error('watchJob is not implemented yet');
}
