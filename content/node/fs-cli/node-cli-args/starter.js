import { parseArgs } from 'node:util';

export function parseCliArgs(argv) {
  // TODO: parse with parseArgs (strict, allowPositionals), then validate
  // every value. Return { ok: true, value } or { ok: false, error }.
  return { ok: false, error: 'parseCliArgs is not implemented yet' };
}
