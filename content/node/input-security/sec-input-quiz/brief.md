Every lesson in this chapter had the same shape: data crossed a boundary,
and code on the other side trusted it. The judgement calls around that
boundary — where to validate, what to escape, which browser mechanisms
actually protect you — come up in every code review of a new endpoint.

These questions are about the reasoning, not the syntax.
