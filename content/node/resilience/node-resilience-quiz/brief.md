Each resilience pattern fixes one failure mode and can cause another:
retries amplify load, timeouts in the wrong order waste work, hedging
doubles writes, fallbacks hide outages. Knowing which tool fits which
failure — and what it costs — is the judgement this chapter is really about.
