The code for a form is the easy part. The decisions around it are where
forms go wrong for users: when an error appears, which attribute keeps a
value out of the request, what the browser's autofill needs from you, and
what stops the same order being placed twice.

These questions are those decisions. Each explanation is the reasoning you
would give in a code review.
